# SocialOps 改造目标

**最后更新：2026-06-01**

## 总目标

将原 AI API 网关项目改造为 **网站总账号池 + 社交账号执行计费平台**。系统只处理总账号池已有账号；非总账号池账号不录入、不绑定、不执行、不计费。

## 当前阶段目标

1. 保留并恢复 SaaS 基础能力：用户、JWT、OAuth、管理员后台、支付、订单、订阅、钱包、兑换码、推广返利、公告、设置、上传、用量/日志框架。
2. 清理 AI 网关业务：provider 路由、模型渠道、AI 账号池、模型计费、Token 用量、AI OAuth、Ops 监控和相关前端入口不得恢复。
3. 建立 SocialOps 最小闭环：总账号池、用户导入匹配、管理员导入/注册入口、分配/收回、管理端执行代理、默认执行代理、执行任务日志和计费挂钩。
4. 真实社交平台注册器和执行器暂不实现；未接入时必须失败关闭，不能伪造成功，不能最终扣费。

## 业务边界

- 用户导入账号只按平台用户名匹配总账号池。X/Twitter 支持 `xxx` 与 `@xxx` 归一为 `@xxx`，大小写不敏感。
- 管理员导入账号直接入总池并按平台用户名去重；真实注册失败不入池。
- 管理员回收账号后，账号回到未分配状态，并清空默认执行代理/代理快照。
- 用户可查看和导出自己账号完整数据，不额外脱敏、不额外应用层加密、不记录导出审计日志。
- IP/代理是用户独有执行资源，但统一由管理端 `/admin/proxies` 管理；同一用户可给多个自己名下账号使用，不能跨用户使用；IP 测试和设置默认代理免费。
- 用户端不保留独立的 `/account-management`、`/account-functions`、`/ip-management` 页面或 `/api/v1/social-ips` API；用户账号查看、导出和执行能力只保留在单一后端 SocialOps 账号接口/后续唯一入口中，不再维护重复执行 UI。
- 只有真实社交执行动作成功后收费，当前统一单价 `0.1`；订阅额度优先，不足扣钱包；失败、未接入、非成功结果不得最终扣费。

## 明确非目标

- 暂不实现购买账号流程、出售/出租状态、自动租期、新钱包、新账本、后台动作单价表。
- 暂不接入真实社交平台执行器或注册器。
- 不恢复 AI 网关、模型路由、Token 计费、provider OAuth/token 刷新、模型/渠道监控。

## 验收口径

验收以 `PROJECT_GUIDE.md` 的业务规则、`.omx/audit/socialops-g001-audit-matrix.md` 的审计矩阵、`.omx/ultragoal/goals.json` 的 G001-G008 状态和最终测试/容器/smoke 证据为准。
